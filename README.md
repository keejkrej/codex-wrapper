# Codex Wrapper

## Parity Baseline

This workspace is converging toward parity with your fork of `t3code`, not directly against the
moving upstream branch.

- Authoritative local checkout: `C:\Users\ctyja\workspace\t3code`
- Authoritative fork remote: `git@github.com:keejkrej/t3code.git`
- Pinned branch: `main`
- Pinned commit SHA: `2a237c20019af8eae1020511b41256ea93127e4c`

This parity baseline applies to the whole workspace:

- `crates/server`
- frontend apps under `apps/`
- shared contracts/packages under `packages/`
- Tauri shells under `apps/*/src-tauri`

If the fork advances, update the pinned SHA here before implementing new parity work.
